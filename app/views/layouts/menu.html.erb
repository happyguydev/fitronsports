<!DOCTYPE html>
<html>
  <head>
    <%= render 'shared/head' %>
  </head>

  <body class="dashboard <%= body_class %>" data-dashboard="false">
    <div class="flex flex-col min-h-screen h-full px-6">
      <div class="<% if show_back_button %> mt-4 <% end %> content">
        <%= yield %>
      </div>
    </div>

    <%= render 'janitors/dashboard/tabs' %>
  </body>
  
  <%= render 'shared/scripts' %>
</html>